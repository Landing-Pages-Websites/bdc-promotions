import type { ReactElement } from "react";

// Placeholder until the prototype port lands (workflow/homepage/c3/PORT.md).
export function VariantD(): ReactElement {
  return (
    <main style={{ minHeight: "70vh", display: "grid", placeItems: "center", padding: "48px 24px", background: "#fff", color: "#0b1220" }}>
      <p>Direction D · Nightfall is being built and will appear here shortly.</p>
    </main>
  );
}
