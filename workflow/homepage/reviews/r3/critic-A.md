# Round 3 (final) — critic:A

## Verdict: PASS
## AI-tell flags: 0
- None. A's composition is locked to image 1, so the AESTHETIC vetoes don't apply to it (3-icon standards row, open FAQ, close band, blue option cards, condensed caps, two hero CTAs). No REJECTED CORRECTNESS pattern appears on the page: no invented stats, stars, logo strips, price toggles or load animation. The capture reports `animatedElements 0`, `gradientElements 0` and `backdropBlurElements 0`.
- I re-checked V181, V187, V188, V190, V200 and V202 at 1536, 1440 and 1280:
  - The next H2 stays out of the 1440×900 fold.
  - The hero CTAs have a gap between them.
  - At 1280 the H1 ink ends at x≈452 and the repo plate starts at x≈525.
  - The scaleX on the proof and close H2s reproduces the comp and is accepted under V200.
- `auto-flags.txt` triage:
  - **23 sizes:** the comp's 20-step ladder plus the consent banner.
  - **Section gap 0px:** a false positive. The bands meet and are separated by surface changes (navy, light, navy, blue, navy), which is SR5 working as intended.
  - **3 shadow recipes:** one plate shadow, one inset hairline on the footer, and Tailwind `shadow-lg` on the shared consent banner. That makes one recipe that belongs to A.

**4-second read (`r3/shots/a-1440-fold.png`):** a car-dealer marketing agency. The promise is more showroom traffic, the proof is the client's own "We Make Luxury Affordable" and repo-sale ads, and the actions are a free audit or a call. It reads in about 2 seconds. Compared with the corpus: Work&Co's fold (`corpus/agencies-design-led/work.co__home/desktop-fold.jpg`) starts its work at y≈472 under one statement. A gives two real ads about 55% of the frame from y≈30, so this fold is more work-forward than the reference.

**The most damaging remaining problem (minor):** on mobile, the growth H2 at 390 and 320 wraps as "ONE CONNECTED PATH / FROM SCROLL TO / SHOWROOM". That leaves one word on the last display line (`pairs/a/mobile-02.png`, `a-320-full.png`). The cause is that at ≤1179px `.line` becomes inline and the heading inherits `text-wrap: pretty`, which does not rebalance a long last word. It is still legible, but it is the one display line on the page that breaks by accident rather than by contract.

## Scores
| Dimension | Score | Evidence (what I see in the screenshot) | Fix |
|---|---|---|---|
| Typography | 8/10 | **Resolved from round 2:** (1) no hard-hyphen breaks anywhere; "follow-up support", "AI-supported" and "vehicle-listing" stay whole at 390 and 320. (2) The source caption is now `max(14px, …)` on desktop and 15px on mobile. (3) The voucher pill in the source composite is about 2× larger and "MESSAGE OR / COMMENT NOW!" is legible at 1536. Reading floors hold at 1280: body, rules, dd and includes text are all ≥16px. **Open:** (1) The growth H2 one-word last line on mobile, described above. (2) At 320, option 01 wraps as "LEAD GEN + / BDC TEAM", leaving the "+" hanging at the end of line 1 (`m320-05`). | (1) In `growth.module.css` ≤1179px, add `.title { text-wrap: balance }`. The heading then breaks as "ONE CONNECTED / PATH FROM SCROLL / TO SHOWROOM" or similar. (2) In `Options.tsx`, put a U+00A0 between "+" and "BDC" so the plus moves to line 2 with its noun. |
| Spacing & rhythm | 8/10 | The desktop bands sit on image 1's coordinates at all three widths. The mobile options band is back on the 20px gutter (cards at x 20–370 at 390). The footer phone and email text now start within 1px of each other (61 vs 60 CSS px). **Open:** (1) At 1280, the divider between standards columns 2 and 3 sits about 10px after "and publication approval" (text ends x≈441, rule at x≈452). The comp has about 22px there. The 16px floor widened the text, and the 166px offset only barely clears it; at 1180 the gap would be about 8px. (2) At 320, the FAQ's Q column takes about 60px, leaving the answers a ~200px measure. "BDC staff and / AI‑supported tools can…" gets a short first line as a result. | (1) In `proof.module.css`, change `.rules li:nth-child(3)` to `left: max(calc(582 * var(--u)), calc(387 * var(--u) + 180px))`. (2) Add `@media (max-width:359px)` that narrows the Q column/indent to 32px. |
| Color & contrast | 9/10 | Recomputed: mint eyebrow on the options band (`#7af4eb` on `#004be1`) 5.16:1; positioning label `#2b76e1` on `#010a1b` 4.51:1 (a pass, but only just); INCLUDES `#1e73fc` 4.58:1; source label `#0563fc` on `#f6f8fb` 4.70:1; caption `#1669de` 4.80:1. The signal slashes (3.04:1), option numerals (3.91:1) and FAQ "Q" (3.59:1) are display-size glyphs, so the 3:1 large-text threshold applies and they pass. The focus ring is 3px cyan and stays above 4:1 on the blue band. The consent Decline button is now `text-neutral-900` / `dark:text-white` with `min-h-11`. | None. If the label ever shifts, move the positioning label to `#3a80e6` for headroom. |
| Layout & hierarchy | 9/10 | Matches the comp section by section in all seven pairs (`r3/pairs/a/01–07`). Proportional scaling holds at 1440 and 1280 with no collisions. The spine alternates loud and quiet: loud hero, loud signal type, light growth band, loud collage, quiet proof, loud blue options, quiet close. | None. |
| Specificity | 9/10 | Real client creative: the Gen-X luxury voucher, the repo sale, the Hub City Ford carousel, the Silverado VLAs, and a disclosure of three more whole ads. Real prices with commitment terms, a real phone number and a real email. It could only be this business. | None. |
| Copy | 9/10 | Word for word with the A deck. No banned words. The non-guarantee appears inline twice (rule 3 and FAQ 4). "Explore the work" is an honest `aria-expanded` disclosure. | None. |
| Restraint | 8/10 | Zero load animation and zero gradients. One plate shadow. The only motion is a 160ms `box-shadow` hover ring and a 160ms arrow rotate, both on interaction, and both killed by `prefers-reduced-motion`. | None for A. The shared consent banner's Tailwind `shadow-lg` is out of A's scope. |
| Mobile | 8/10 | **The round-2 blocker is resolved.** At 390×844 the slab starts at y=685.5 and the luxury plate at y=713, so about 131px of creative is in the fold, including the whole "WE MAKE LUXURY AFFORDABLE" lockup. The proof box now follows the art. At 320×700 the call CTA ends at about y≈644, inside the fold. The 320 arrow and label collisions are gone: EXPLORE, SEE HOW and FIND THE RIGHT MIX all have clear gaps (`m320-04/05/06`). There is no overflow, body text is ≥17px, and targets are ≥44px. **Open:** at 320×700 only 26px of blue slab reaches the fold and no creative does. That is the right trade-off (the CTAs come first), but it is still zero. | Optional: at `max-width:359px`, set the hero body to 16px/1.45 and `.slab{margin-top:16px}`. That buys about 40px and brings the "WE MAKE" line into a 320×700 fold. |
| 4s/40s test | 9/10 | **4s:** a dealer agency, more showroom traffic, audit or call. **40s:** signal, then the 5-step lane, then the inspectable work, then the standards, then published prices, then the close with the non-guarantee. It is an argument with a conclusion. | None. |
| Brief adherence | 9/10 | Every locked element is present: slanted slab, front plate and tilted plate; the cyan route; the staircase; the source card; the four-plate collage with connectors; the outlined proof panel with the headlight; the wide first option card; the open FAQ. The storyboard label moved outside the plate, the VLA is cropped and the Meta plate sits on navy, all logged in DEVIATIONS. The open-state production captures now exist (`reviews/r2/fix-a/explore-open-1536.png`, `explore-open-390.png`). The three labels share one baseline and the dev badge is gone. | Copy the two explore-open captures into `reviews/r3/shots/` so the frozen set is complete. |

## Top 3 changes, in order of impact
1. Add `text-wrap: balance` to the growth H2 at ≤1179px so no display line ends on a lone "SHOWROOM". Also add a non-breaking space in "+ BDC" so the "+" doesn't hang at 320.
2. Widen the column-3 standards offset to `+180px` so the divider keeps about 20px from column 2's longest line at 1180–1300.
3. At ≤359px, narrow the FAQ Q indent to 32px and tighten the hero body and slab top margin by about 40px, so the "WE MAKE" line enters the 320×700 fold.

## What is already good (keep): freeze notes for `approved/`
- **Desktop reproduction.** Every section matches image 1 at 1536, and scaling is proportional (`--u = min(100cqw,1920px)/1536`) down to 1280 with no headline or plate collision (V202 checked by measurement).
- **Mobile hero order.** Eyebrow, H1 (image 1's four-line break), body, audit, call, then the art row, then the proof box. About 131px of real creative is in the 390 fold, and the call CTA is in the 320×700 fold.
- **Reading and target floors.** `max(16px, …)` on every reading paragraph and `max(12px, …)` on labels. Every CTA is ≥44px. Footer links are `min-height:44px`.
- **Interaction.** One hover signal on every CTA (a 3px inset cyan ring, 160ms), a 3px cyan focus outline with a 3px offset, and a `prefers-reduced-motion` kill switch. "Explore the work" is an `aria-expanded` disclosure of three whole supplied ads whose labels share a bottom line.
- **Honesty and evidence.** Prices and commitment terms are in full (SR9). The non-guarantee is inline in both the standards and the FAQ. There are no metrics, logos or testimonials.
- **Round-2 status.** Resolved: mobile-fold creative, the 320 arrow collisions, non-breaking hyphens, the 14px caption floor, the explore-panel baseline, the options gutter, the footer icon box, the voucher pill scale, the open-state captures, and the consent Decline contrast and height. Partly open: 320×700 creative (0px, a minor trade-off).

## Route to C (not failures of A)
- The standards slot pairs an AI-generated B&W headlight with "PROOF YOU CAN INSPECT". C should put inspectable client work there, or nothing.
- The storyboard plate reads as unreadable document texture at 390 (`mobile-04`). C could show one scene large, with the script line set as real text.
- The work intro names testimonial, employee and viral pieces that no plate shows. C's contact sheet should label every creative type it claims.
- On mobile, A still spends about 680px on type before the work. C can lead with the work on mobile, as Instrument's 390 fold does (`corpus/agencies-design-led/instrument.com__home/mobile-fold.jpg`).

Evidence: `/Users/syedali/dev projects/Gomega Websites/automated builds/BDC promotions/workflow/homepage/reviews/r3/shots/` and `…/r3/pairs/a/`. The 320 tiles and crops are in `/private/tmp/claude-501/-Users-syedali-dev-projects-Gomega-Websites-automated-builds-BDC-promotions/0e2c338a-761d-4760-a3ff-83e06a922e36/scratchpad/c3/` (`m320-00…07.png`, `p1280-proof.png`, `p1280-work.png`, `src1536.png`).